{
  "name": "demo",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "svelte-kit dev",
    "build:local": "svelte-kit build",
    "build": "BASE=true svelte-kit build",
    "deploy": "touch build/.nojekyll && npx gh-pages -d build -t",
    "start": "svelte-kit start"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "next",
    "@sveltejs/kit": "next",
    "carbon-components-svelte": "^0.39.0",
    "carbon-preprocess-svelte": "^0.5.0",
    "svelte": "^3.38.3",
    "vite": "^2.4.0"
  }
}
